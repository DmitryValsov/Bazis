 <style>
    /* =============== Переменные =============== */
    :root {
      --clr-bg-dark:       #082d5f;
       --clr-bg-light:      #FFFFFF;
      --clr-primary-start: #6366F1;
      --clr-primary-end:   #4F46E5;
      --clr-text-white:    #FFFFFF;
      --clr-text-light:    rgba(255,255,255,0.8);
      --clr-text-dark:     #111827;
      --clr-text-gray:     #6B7280;

      --r-sm: 12px;
      --gap-xs: 4px;
      --gap-sm: 8px;
      --gap-md: 16px;
      --gap-lg: 24px;
      --ff: 'Inter', sans-serif;
      --shadow-card: 0 2px 6px rgba(0,0,0,0.05);
    }

    /* =============== Сброс =============== */
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }
    html, body {
      height: 100%;
    }
    body {
      font-family: var(--ff);
      background: var(--clr-bg-dark);
      color: var(--clr-text-white);
      /* убрали flex-контейнер, чтобы скроллилась вся страница */
    }
    a { text-decoration: none; color: inherit; }
    img { max-width: 100%; display: block; }

    /* =============== Большой хедер (hero) =============== */
    .hero {
      padding: env(safe-area-inset-top) var(--gap-md) var(--gap-lg);
      background: var(--clr-bg-dark);
    }
    .hero__header {
      display: flex;
      align-items: center;
      gap: var(--gap-sm);
    }
    .hero__logo {
      width: 32px;
      height: 32px;
      background: var(--clr-primary-start);
      border-radius: var(--r-sm);
    }
    .hero__title {
      font-size: 18px;
      font-weight: 500;
    }
    .hero__main-title {
      margin-top: var(--gap-lg);
      font-size: 32px;
      font-weight: 700;
      line-height: 1.1;
    }
    .hero__subtitle {
      margin-top: var(--gap-xs);
      font-size: 15px;
      color: var(--clr-text-light);
    }
    .hero__btn {
      display: block;
      margin-top: var(--gap-lg);
      width: 100%;
      padding: 14px 0;
      font-size: 16px;
      font-weight: 600;
      text-align: center;
      color: var(--clr-text-white);
      background: linear-gradient(90deg, var(--clr-primary-start), var(--clr-primary-end));
      border: none;
      border-radius: var(--r-sm);
      cursor: pointer;
    }

    /* =============== Малый фиксированный хедер =============== */
    .small-header {
      position: fixed;
      top: 0;
      left: 0;
      right: 0;
      display: flex;
      justify-content: space-between;
      align-items: center;
      padding: env(safe-area-inset-top) var(--gap-md) 8px;
      background: var(--clr-bg-dark);
      z-index: 100;
      opacity: 0;
      pointer-events: none;
      transition: opacity .3s;
    }
    .small-header.visible {
      opacity: 1;
      pointer-events: auto;
    }
    .small-header button {
      background: none;
      border: none;
      padding: 8px;
      display: flex;
      align-items: center;
      justify-content: center;
      cursor: pointer;
    }
    .small-header svg {
      width: 24px;
      height: 24px;
      fill: var(--clr-text-white);
    }
    .small-header .hero__title {
      font-size: 18px;
      font-weight: 500;
    }

    /* =============== Контент-контейнер =============== */
    .content {
      background: var(--clr-bg-light);
      color: var(--clr-text-dark);
      padding: var(--gap-lg) var(--gap-md) calc(var(--gap-lg) + env(safe-area-inset-bottom));
      /* скроллит вся страница, поэтому overflow у контента не нужен */
    }
    .section {
      margin-bottom: var(--gap-lg);
    }
    .section__header {
      display: flex;
      justify-content: space-between;
      align-items: center;
      margin-bottom: var(--gap-md);
    }
    .section__header h2 {
      font-size: 20px;
      font-weight: 600;
    }
    .section__header a {
      font-size: 14px;
      color: var(--clr-primary-start);
    }

    /* === Автомобили: карточки списка === */
    .car-list {
      display: flex;
      flex-direction: column;
      gap: var(--gap-md);
    }
    .car-item {
      display: flex;
      background: var(--clr-bg-light);
      border-radius: var(--r-sm);
      box-shadow: var(--shadow-card);
      overflow: hidden;
    }
    .car-item__img {
      width: 120px;
      height: 120px;
      object-fit: cover;
      flex-shrink: 0;
    }
    .car-item__body {
      flex: 1;
      padding: var(--gap-md);
      display: flex;
      flex-direction: column;
      justify-content: space-between;
    }
    .car-item__title {
      font-size: 18px;
      font-weight: 600;
      margin-bottom: 4px;
    }
    .car-item__year,
    .car-item__vin {
      font-size: 14px;
      color: var(--clr-text-gray);
      margin-bottom: 8px;
    }
    .btn-secondary {
      display: inline-flex;
      align-items: center;
      gap: 4px;
      padding: 6px 12px;
      font-size: 14px;
      background: #F3F4F6;
      color: var(--clr-text-dark);
      border: none;
      border-radius: var(--r-sm);
      cursor: pointer;
    }
    .btn-secondary svg {
      width: 16px;
      height: 16px;
      fill: var(--clr-text-gray);
    }

    /* === Кнопка "Добавить автомобиль" === */
    .btn-add {
      display: block;
      margin: var(--gap-lg) auto;
      max-width: 300px;
      width: 100%;
      padding: 14px 0;
      font-size: 16px;
      font-weight: 600;
      text-align: center;
      color: var(--clr-text-white);
      background: var(--clr-bg-dark);
      border: none;
      border-radius: var(--r-sm);
      cursor: pointer;
    }

    /* === История обслуживания === */
    .history {
      margin-top: var(--gap-lg);
    }
    .history__header {
      font-size: 20px;
      font-weight: 600;
      margin-bottom: var(--gap-md);
    }
    .history-list {
      display: flex;
      flex-direction: column;
      gap: var(--gap-md);
    }
    .history-item {
      display: flex;
      align-items: center;
      justify-content: space-between;
      background: var(--clr-bg-light);
      border-radius: var(--r-sm);
      box-shadow: var(--shadow-card);
      padding: var(--gap-md);
    }
    .history-item__left {
      display: flex;
      align-items: center;
      gap: var(--gap-md);
    }
    .history-item__icon {
      width: 40px;
      height: 40px;
      background: #F3F4F6;
      border-radius: var(--r-sm);
      display: flex;
      align-items: center;
      justify-content: center;
      flex-shrink: 0;
    }
    .history-item__icon svg {
      width: 20px;
      height: 20px;
      fill: var(--clr-text-gray);
    }
    .history-item__info {
      display: flex;
      flex-direction: column;
      line-height: 1.2;
    }
    .history-item__title {
      font-size: 16px;
      font-weight: 600;
    }
    .history-item__subtitle {
      font-size: 14px;
      color: var(--clr-text-gray);
      margin-top: 4px;
    }
    .history-item__km {
      font-size: 14px;
      color: var(--clr-text-dark);
      flex-shrink: 0;
    }

  
  </style>

  <!-- Большой хедер -->
  <div class="hero" id="hero">
    <div class="hero__header">
      <div class="hero__logo"></div>
      <div class="hero__title">Базис Моторс</div>
    </div>
    <div class="hero__main-title">Мои автомобили</div>
    <div class="hero__subtitle">Управляйте своими авто и историей обслуживания.</div>
    <button class="hero__btn">+ Добавить автомобиль</button>
  </div>

  <!-- Малый фиксированный хедер -->
  <div class="small-header" id="smallHeader">
    <button aria-label="Меню">
      <svg viewBox="0 0 24 24"><path d="M3 6h18M3 12h18M3 18h18" stroke="currentColor" stroke-width="2" stroke-linecap="round"/></svg>
    </button>
    <div class="hero__title">Мои автомобили</div>
    <button aria-label="Профиль">
      <svg viewBox="0 0 24 24"><path d="M12 12a5 5 0 100-10 5 5 0 000 10zm-7 8a7 7 0 0114 0H5z"/></svg>
    </button>
  </div>

  <!-- Основной контент -->
  <div class="content">

    <!-- Мои автомобили -->
    <section class="section">
      <div class="section__header">
        <h2>Мои автомобили</h2>
        <a href="#">Все</a>
      </div>
      <div class="car-list">
        <div class="car-item">
          <img src="/img/news/news.jpeg" alt="Chery Tiggo 8 Pro" class="car-item__img">
          <div class="car-item__body">
            <div>
              <div class="car-item__title">Chery Tiggo 8 Pro</div>
              <div class="car-item__year">2022</div>
              <div class="car-item__vin">VIN 1234567890ABCDEFG</div>
            </div>
            <button class="btn-secondary">
              История обслуживания
              <svg viewBox="0 0 24 24"><path d="M9 18l6-6-6-6"/></svg>
            </button>
          </div>
        </div>
        <div class="car-item">
          <img src="/img/news/news.jpeg" alt="Changan CS85 Coupe" class="car-item__img">
          <div class="car-item__body">
            <div>
              <div class="car-item__title">Changan CS85 Coupe</div>
              <div class="car-item__year">2023</div>
              <div class="car-item__vin">VIN 9876543210QWERTYU</div>
            </div>
            <button class="btn-secondary">
              История обслуживания
              <svg viewBox="0 0 24 24"><path d="M9 18l6-6-6-6"/></svg>
            </button>
          </div>
        </div>
      </div>

      <!--<button class="btn-add">+ Добавить автомобиль</button>-->
    </section>

    <!-- История обслуживания выбранного авто -->
    <section class="section history">
      <div class="history__header">Chery Tiggo 8 Pro, 2022</div>
      <div class="history-list">
        <div class="history-item">
          <div class="history-item__left">
            <div class="history-item__icon">
              <svg viewBox="0 0 24 24"><path d="M6 2h1v2H6V2zm10 0h1v2h-1V2zm-10 4h12v14H6V6z"/></svg>
            </div>
            <div class="history-item__info">
              <div class="history-item__title">Замена масла</div>
              <div class="history-item__subtitle">Выполнено 30 150 км</div>
            </div>
          </div>
          <div class="history-item__km">30 150 км</div>
        </div>

        <div class="history-item">
          <div class="history-item__left">
            <div class="history-item__icon">
              <svg viewBox="0 0 24 24"><path d="M6 2h1v2H6V2zm10 0h1v2h-1V2zm-10 4h12v14H6V6z"/></svg>
            </div>
            <div class="history-item__info">
              <div class="history-item__title">Сезонная замена шин</div>
              <div class="history-item__subtitle">Выполнено 26 300 км</div>
            </div>
          </div>
          <div class="history-item__km">26 300 км</div>
        </div>

        <div class="history-item">
          <div class="history-item__left">
            <div class="history-item__icon">
              <svg viewBox="0 0 24 24"><path d="M6 2h1v2H6V2zm10 0h1v2h-1V2zm-10 4h12v14H6V6z"/></svg>
            </div>
            <div class="history-item__info">
              <div class="history-item__title">Замена стекловых стёкол</div>
              <div class="history-item__subtitle">Выполнено 25 900 км</div>
            </div>
          </div>
          <div class="history-item__km">25 900 км</div>
        </div>
      </div>
    </section>

  </div>

  

  <script>
    const hero = document.getElementById('hero');
    const smallHdr = document.getElementById('smallHeader');

    window.addEventListener('scroll', () => {
      smallHdr.classList.toggle('visible', window.scrollY >= hero.offsetHeight);
    });
  </script>


